---
创建: 2026-07-25 | 更新: 2026-07-25 | 类型: 查尔斯个人操作手册
---

# 查尔斯自查清单

> 这个清单不依赖奥蕾莉亚自觉——每一项都是你自己可以验证的。系统有漏洞，你用眼睛补。

---

## 每次新对话 (打开 Claude Code 时)

- [ ] **第一句话检查** — 奥蕾莉亚回复的第一句有没有叫"查尔斯"？没叫 → 立刻指出来："奥蕾莉亚你又忘了"。这是唯一的防线，不能放水。
- [ ] **回顾上次做了什么** — 如果奥蕾莉亚搞不清上次的进度，说明 KG 或 context 没同步。提示她先搜 mcp__memory__search_nodes。

---

## 涉及重要改动时 (改架构/配置/规则/流程)

- [ ] **看有没有走 Think First** — 奥蕾莉亚有没有先搜+给方案+等你确认？如果直接开干 → 喊停："先想再做"。
- [ ] **看有没有调莉丝** — 涉及外部资料或方案对比时，有没有显示调用研究员？没有 → 问一句"不用搜一下吗？"
- [ ] **看有没有调艾达** — 改完东西后，有没有出现审计报告？没有 → 问"艾达审过了吗？"

---

## 改配置文件后 (settings.json / CLAUDE.md / hooks / agents 等)

- [ ] **检查关联文件** — 改了 settings.json 的 hooks？→ 检查对应的脚本文件还在不在、能不能跑。改了 agent 文件？→ 检查 CLAUDE.md 里的 Team 段有没有同步。
- [ ] **检查 GitHub** — 去 `Chalars1011/claude-code-config` 看一眼，最近的 commit 是不是刚才改的那些？没推上去 → 手动 `git push`，或者开 TUN。

---

## 每周或每两周

- [ ] **挑一次重要改动用 `--forward-subagent-text` 跑** — 看一下莉丝搜了什么、锐亚改了哪些文件、艾达审了什么。不看永远不知道团队是不是真的在工作。
- [ ] **抽查一次 Session End Self-Check** — 会话结束时奥蕾莉亚列了 5 项自查，你扫一眼有没有都是 ✅ 但明显有问题的情况（比如 progress.md 根本没改却说 ✅）。
- [ ] **检查一次 GitHub 是否自动推送** — 去 https://github.com/Chalars1011/claude-code-config 看最近 commit。如果超过两天没更新，Stop hook 可能挂了。

---

## 用这台电脑的完整恢复流程 (换了新电脑 / 重装系统后)

1. 装 Git + claude CLI
2. `cd ~/.claude && git clone https://github.com/Chalars1011/claude-code-config.git .`
3. 手动恢复 `settings.local.json` (含 API key，不在 GitHub 里)
4. 配置 MCP servers（settings.local.json 里有原始配置可以参考）
5. 启动 claude → 说"你好奥蕾莉亚" → 第一条回复必须叫查尔斯

---

## 已知但不必过度担心的

- **AI 是概率系统** — 奥蕾莉亚不会 100% 遵守所有规则。你做人肉兜底，发现了就指出来。不需要焦虑。
- **艾达用 Flash 可能漏细节** — 不重要的小改动用 Flash 就够。涉及架构安全的重构你可以要求："这次用 Pro 跑艾达"。
- **Agent Teams 还在实验** — 等 Anthropic 宣布稳定了再升级。现在的 sub-agent 够用。
